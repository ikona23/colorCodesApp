var test = require('tape')
var db = require('../data/db')

//act

// 1. How would like to use the function
// 2. Internals. Given I know how it will be used
// 3. How can I make that work

//If i want to parse only object dont need to use parse, parse are used only with the string
 test('it gets a specific cat by id', function (t) {
   var expColor = {
     "id":2,
     "name": "red",
     "image": "http"

   console.log({});
     //I have to ge re
   }
